# Cookie Orders with Validation

- [x] 1 Create a new schema and table for cookie orders and forward engineer
- [x] 2 Set up a new modularized project, with CRUD functionality as shown above
  - [x] 2.1 Validations: All fields are required
  - [x] 2.2 If there are any errors, redirect back to '/cookies/new' again to show the errors
  - [x] 2.3 Clicking the Log button saves a cookie order and redirects to the orders dashboard, unless there are errors
- [ ] 3 The Change/Edit order page will use the same validations as the Create/New order page
  - [ ] 3.1 Clicking the log button updates a cookiee order and redirects to the orders dashboard, unless there are errors